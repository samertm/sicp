Because of applicative-order processing, all the parts of the new process are evaluated even if they aren't used. This means that the new-if function evaluates the then-clause and the else-clause, even though only one is 'used'.
